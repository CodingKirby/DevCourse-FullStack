// 방법 1: 함수 선언(Function Declaration)
function add1 (x, y) {
    return x + y
}

// 방법 2: 함수 표현식(Function Expression) 
let add2 = function (x, y) {
    return x + y
}

// 방법 3: 화살표 함수(Arrow Function) 
let add3 = (x, y) => {
    return x + y
}

let add4 = (x, y) => x + y